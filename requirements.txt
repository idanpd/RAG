# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
sqlite3  # Built-in with Python
pathlib  # Built-in with Python
logging  # Built-in with Python

# Configuration
PyYAML>=6.0

# Embeddings and Search
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0  # Use faiss-gpu if you have GPU
transformers>=4.20.0

# Optional: BM25 for sparse retrieval
rank-bm25>=0.2.2

# Optional: Cross-encoder for reranking
# (included in sentence-transformers)

# Text extraction
PyMuPDF>=1.20.0  # For PDF extraction
python-docx>=0.8.11  # For DOCX extraction
python-pptx>=0.6.21  # For PPTX extraction
openpyxl>=3.0.10  # For Excel extraction

# Image processing
Pillow>=9.0.0
pytesseract>=0.3.10  # Requires tesseract-ocr system package

# Video processing (optional)
opencv-python>=4.6.0  # For video keyframe extraction

# LLM support (CPU optimized)
llama-cpp-python>=0.2.0  # For local LLM inference - compile with CPU optimizations
torch>=1.12.0+cpu  # CPU-only PyTorch for smaller download
requests>=2.28.0  # For Ollama API and model downloads

# Optional: Token counting
tiktoken>=0.4.0

# Web UI
streamlit>=1.28.0

# Development/Testing (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0